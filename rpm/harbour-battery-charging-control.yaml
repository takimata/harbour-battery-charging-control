Name: harbour-battery-charging-control
Summary: Controls battery charging
Version: 0.1
Release: 1
# The contents of the Group field should be one of the groups listed here:
# http://gitorious.org/meego-developer-tools/spectacle/blobs/master/data/GROUPS
Group: Qt/Qt
URL: https://github.com/takimata/harbour-battery-charging-control
License: GPL-3.0
# This must be generated before uploading a package to a remote build service.
# Usually this line does not need to be modified.
Sources:
- '%{name}-%{version}.tar.bz2'
Description: |
  This application allows to disable battery charging depending on the battery level.
Configure: none
# The qtc5 builder inserts macros to allow QtCreator to have fine
# control over qmake/make execution
Builder: qtc5

# This section specifies build dependencies that are resolved using pkgconfig.
# This is the preferred way of specifying build dependencies for your package.
PkgConfigBR:
  - sailfishapp >= 1.0.2
  - Qt5Core
  - Qt5Qml
  - Qt5Quick
  - contextkit-statefs >= 0.2.7

# Build dependencies without a pkgconfig setup can be listed here
PkgBR:
    - libkeepalive-devel

# Runtime dependencies which are not automatically detected
Requires:
  - sailfishsilica-qt5 >= 0.10.9
  - libkeepalive

# All installed files
# list stuff in '%{_datadir}/%{name}/ explicitly to avoid warning while builing rpm (also see .spec)
Files:
  - '%{_bindir}'
  - '%{_datadir}/%{name}/images'
  - '%{_datadir}/%{name}/qml'
  - '%{_datadir}/%{name}/translations'
  - '%{_datadir}/applications/%{name}.desktop'
  - '%{_datadir}/icons/hicolor/*/apps/%{name}.png'

# For more information about yaml and what's supported in Sailfish OS
# build system, please see https://wiki.merproject.org/wiki/Spectacle
